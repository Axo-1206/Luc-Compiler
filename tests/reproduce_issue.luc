package test

const core_add (a int) (b int) int = {
    return a 
}

export const main () int = {
    let myAdd (a int) int = core_add(5)
    let res1 int -- must throw error, the res1 is not nullable so it must be intialize with a certain value
    return 0
}
